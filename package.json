{
  "name": "turn0",
  "version": "1.0.0",
  "private": true,
  "description": "TURN0: sistema de gestion de filas y citas para pequenos negocios.",
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "validate": "npm run build --prefix frontend",
    "deploy:frontend": "npm run build --prefix frontend",
    "deploy:backend": "npm run start --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^9.2.0"
  }
}
